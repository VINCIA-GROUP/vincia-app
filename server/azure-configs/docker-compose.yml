version: '3.4'

services:
  vincia.api:
      container_name: vincia-api
      image: igorcoura/vincia-api
      restart: always
      environment:
          API_ENVIRONMENT: 
          AUTH0_DOMAIN: 
          AUDIENCE: 
          APP_SECRET_KEY: 
          CONNECTION_STRING_DB: 
          OPENAI_KEY: 
      dns:
          - 8.8.8.8
